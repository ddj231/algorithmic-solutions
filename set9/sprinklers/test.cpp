
// use allocator funtion to get a pointer to our new page table
// check that the returned pointer to pagetable is not null

//virtual_memory_lookup
// pagetable, addr
// returns vamapping struct


// for page_addr in virtual_memory_pages:
//      mapping = virtual_memory_lookup(passed_in_pagetable, page_addr)
//      virtual_memory_map(new_pagetable, page_addr, mapping.pa, PAGESIZE, mapping.perm)
//
//retun new_pagetable

